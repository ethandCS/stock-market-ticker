{
    "functions": {
      "api/*.py": {
        "memory": 1024,
        "maxDuration": 10
      }
    },
    "builds": [
      { "src": "backend/app.py", "use": "@vercel/python" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "backend/app.py" }
    ]
  }
  